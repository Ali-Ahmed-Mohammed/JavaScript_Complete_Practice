

// Todo: Operators

// Todo: 1: Arithmetic Operators: are used to perform arithmetic on numbers

// the numbers in arithmetic are called operands

// the operation between the two operands is defined by an operator 

// a: Assignment : Assigns a value to a varible

let x = 10;

// 2: Addition : adds numbers

let y = 2
let add = x + y;  

// b: Multiplication: multiplies numbers

let multiply = x * y;

// c: Division: divide numbers

let divide = x / y;

// d: subtraction: subtract numbers

let subtract = x - y;

// e: modulus: holds the division remainder

let remainder = x % y;

// f: Exponentiation: raises the first operand to the power of the second operand

let power = x ** y  // 10^2

// g: increment : increments numbers

x++  // means x = x + 1

// h: decrement: decrements numbers

y--  // means y = y - 1


//  Todo: Operator Precedence

/*
Operator precedence describes the order in which operations are performed in an arithmetic expression
*/

var result = 100 + 50 * 2    // 300
var result = 100 + (50 * 2)  // 200

// Todo: 2: Assignment Operators: Assigns values to javaScript Values

// a: equal operator: Assigns a value to a variable

let assisgnValue = 'value';





